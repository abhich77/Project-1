1.create project template by executing template.py file
2.write code on setup.py and pyproject.toml to import local packages
3.create virtual env,activate it and install requirements from requirements.txt
  conda create -n vehicle python=3.14 -y
  conda activate vehicle
  add required modules to requirements.txt
  do "pip install -r requirements.txt"

  4.do a "pip list" on terminal to make sure you have local packages installed.

  mongo db setup

  5.sign in to mongo db atlas and create new project
  6.create a new cluster
  7.setup username and password and create DB user "abhirajcug23ee_db_user" "pHVxmLDFopdSxfmi" "mongodb+srv://abhirajcug23ee_db_user:pHVxmLDFopdSxfmi@cluster0.4boagp7.mongodb.net/?appName=Cluster0"
  8. go to network access and add "0.0.0.0/0" so we can access from anywhere
  9.hit on get connection string and select python
  10.copy and save connection string
  11.create a new folder notebook>>create file>> mongo_db.input
  12.add data set to notebook and create a file mongodb_demo.ipynb select vehicle as kernel
  13.push data to mongo db 
  14.go to mongodb atlas >>database>>browse connection>>see your data
  
  
  15.write thr logger file and test it
  16.write the exception file and test it
  17.EDA and feature engg. notebook added.